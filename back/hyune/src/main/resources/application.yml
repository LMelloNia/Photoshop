server:
  port: 8282

spring:
    dataSource:
        url: jdbc:mysql://localhost:3306/scottdb
        username: root
        password: 12341234
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                '[format_sql]': true
    # servlet:
    #     multipart:
    #         max-request-size: 30MB
    #         max-file-size: 10MB